// Placeholder shell — Task 2 builds the real App (header + tabs + Dashboard).
// Kept minimal so Task 1 build gate passes.
export default function App() {
  return (
    <div className="mx-auto max-w-3xl p-6">
      <h1 className="text-2xl font-bold text-gray-900">Freelance Wallet</h1>
      <p className="mt-2 text-sm text-gray-500">
        Scaffold Task 1 OK — Dashboard và các trang CRUD ở Task 2/3.
      </p>
    </div>
  );
}
